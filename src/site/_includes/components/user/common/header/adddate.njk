{% set grayMatter = require("gray-matter") %}
{% set pagematter = grayMatter(page.inputContent) %}

<p>
Publish on {{ pagematter.data.date }}   
</p>

<p>
Last updated on {% if pagematter.data.lastmod %} {{ pagematter.data.lastmod }} {% endif %}
</p>

